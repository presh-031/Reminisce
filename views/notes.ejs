<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head.ejs') %>
  </head>
  <body>
    <!-- header may use partial or be custom  -->
    <header class="flex flex-col items-center gap-4 bg-black py-4">
      <p class="text-4xl font-semibold text-[#46d7ff]">Reminisce</p>
      <ul class="flex items-center gap-8 text-xl text-white">
        <li><a href="/logout">LogOut</a></li>
      </ul>
    </header>

    <h2><%= user.userName.charAt(0).toUpperCase() + user.userName.slice(1) %> has <%= left %> notes left.</h2>
    <!-- show the number of read and unread notes -->
    <%- include('./partials/footer.ejs') %>
  </body>
</html>
